# DecisionTree_Classfier_and_Reg

Decision tree classification, regression, and hyperparameter-tuning examples.

## Tests

Install the project and test dependencies, then run the coverage suite:

```bash
pip install -r requirements.txt -r requirements-dev.txt
pytest
```